// Take the solution of the previous task Show and alert. Modify it by extracting the script content into an external file alert.js, residing in the same folder.

// alert("Im loving it!");

var name = prompt("Enter your name:");
alert("Hello " + name)